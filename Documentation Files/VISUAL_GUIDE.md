# 📊 Visual Workflow Guide

## 🎯 Complete System Workflow

### Step 1: Initial Data Sync
```
┌─────────────┐
│    USER     │
│  (Browser)  │
└──────┬──────┘
       │ Clicks "Sync Data"
       ▼
┌──────────────────────┐
│  React Frontend      │
│  POST /api/scrape/   │
│      sync            │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  FastAPI Backend                     │
│                                      │
│  ┌────────────────────────────────┐ │
│  │  AGENT 1: Data Scraper         │ │
│  │                                │ │
│  │  1. HTTP GET to website        │ │
│  │  2. Parse HTML with BS4        │ │
│  │  3. Extract price data         │ │
│  │  4. Check existing dates       │ │
│  │  5. Insert new records         │ │
│  │  6. Update existing records    │ │
│  └────────┬───────────────────────┘ │
│           │                          │
│           ▼                          │
│  ┌────────────────────────────────┐ │
│  │  SQLite Database               │ │
│  │  - gold_prices table           │ │
│  │  - price_alerts table          │ │
│  └────────────────────────────────┘ │
└──────────────────────────────────────┘
       │
       │ Returns: { new_records: X, updated_records: Y }
       ▼
┌──────────────────────┐
│  React Frontend      │
│  Shows success msg   │
└──────────────────────┘
```

### Step 2: Viewing Price Charts
```
┌─────────────┐
│    USER     │
│  Selects    │
│ "3 Months"  │
└──────┬──────┘
       │
       ▼
┌──────────────────────┐
│  React Frontend      │
│  GET /api/prices/    │
│    period/3months    │
└──────┬───────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  FastAPI Backend                     │
│                                      │
│  ┌────────────────────────────────┐ │
│  │  AGENT 2: Data Provider        │ │
│  │                                │ │
│  │  1. Calculate date range       │ │
│  │     (today - 90 days)          │ │
│  │  2. Query database             │ │
│  │     WHERE date >= start_date   │ │
│  │  3. Convert to JSON            │ │
│  │  4. Return array of records    │ │
│  └────────┬───────────────────────┘ │
│           │                          │
│           ▼                          │
│  ┌────────────────────────────────┐ │
│  │  Database Query Result         │ │
│  │  [                             │ │
│  │    {date, carat_22_1gram, ...},│ │
│  │    {date, carat_22_1gram, ...},│ │
│  │    ...                         │ │
│  │  ]                             │ │
│  └────────────────────────────────┘ │
└──────────────────────────────────────┘
       │
       │ Returns: Array of price records
       ▼
┌──────────────────────┐
│  React Frontend      │
│  - Updates state     │
│  - Recharts renders  │
│  - Shows chart       │
└──────────────────────┘
```

### Step 3: Alert Generation
```
┌─────────────┐
│    USER     │
│   Visits    │
│  Dashboard  │
└──────┬──────┘
       │
       ▼
┌──────────────────────┐
│  React Frontend      │
│  GET /api/tracking/  │
│      all             │
└──────┬───────────────┘
       │
       ▼
┌────────────────────────────────────────────────┐
│  FastAPI Backend                               │
│                                                │
│  ┌──────────────────────────────────────────┐ │
│  │  AGENT 3: Price Tracker                  │ │
│  │                                          │ │
│  │  1. Get latest price from DB            │ │
│  │     (e.g., Rs. 40,920)                  │ │
│  │                                          │ │
│  │  2. Get price from 7 days ago           │ │
│  │     (e.g., Rs. 39,000)                  │ │
│  │                                          │ │
│  │  3. Calculate weekly % change           │ │
│  │     ((40920 - 39000) / 39000) * 100     │ │
│  │     = +4.92%                            │ │
│  │                                          │ │
│  │  4. Check threshold                     │ │
│  │     4.92% < 10% (increase threshold)    │ │
│  │     4.92% > -5% (decrease threshold)    │ │
│  │     → NOT CRITICAL                      │ │
│  │                                          │ │
│  │  5. Get price from 30 days ago          │ │
│  │     (e.g., Rs. 36,000)                  │ │
│  │                                          │ │
│  │  6. Calculate monthly % change          │ │
│  │     ((40920 - 36000) / 36000) * 100     │ │
│  │     = +13.67%                           │ │
│  │                                          │ │
│  │  7. Check threshold                     │ │
│  │     13.67% >= 10% (increase threshold)  │ │
│  │     → CRITICAL ALERT! 🚀                │ │
│  │                                          │ │
│  │  8. Generate alert message              │ │
│  │     "CRITICAL: Price increased 13.67%"  │ │
│  │                                          │ │
│  │  9. Save alert to database              │ │
│  │                                          │ │
│  │  10. Return comprehensive data          │ │
│  └──────────┬───────────────────────────────┘ │
│             │                                  │
│             ▼                                  │
│  ┌──────────────────────────────────────────┐ │
│  │  Response Data                           │ │
│  │  {                                       │ │
│  │    weekly_tracking: {                    │ │
│  │      percentage_change: 4.92,            │ │
│  │      is_critical: false                  │ │
│  │    },                                    │ │
│  │    monthly_tracking: {                   │ │
│  │      percentage_change: 13.67,           │ │
│  │      is_critical: true                   │ │
│  │    },                                    │ │
│  │    alerts: [                             │ │
│  │      {                                   │ │
│  │        severity: "info",                 │ │
│  │        message: "Weekly: +4.92%"         │ │
│  │      },                                  │ │
│  │      {                                   │ │
│  │        severity: "critical",             │ │
│  │        message: "CRITICAL: +13.67%"      │ │
│  │      }                                   │ │
│  │    ]                                     │ │
│  │  }                                       │ │
│  └──────────────────────────────────────────┘ │
└────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  React Frontend                      │
│                                      │
│  ┌────────────────────────────────┐ │
│  │  Blue Alert (Info)             │ │
│  │  ℹ️ Weekly: +4.92%             │ │
│  └────────────────────────────────┘ │
│                                      │
│  ┌────────────────────────────────┐ │
│  │  Green Alert (Critical)        │ │
│  │  🚀 CRITICAL: +13.67%          │ │
│  └────────────────────────────────┘ │
└──────────────────────────────────────┘
```

## 🔄 Real-Time Data Flow

### Continuous Monitoring Cycle
```
    ┌──────────────────────────────────────┐
    │         USER DASHBOARD               │
    │                                      │
    │  [Sync Data] ←────── Every time     │
    │       ↓              user wants      │
    │       │              fresh data      │
    │       ▼                              │
    │  ┌─────────────────────────────┐    │
    │  │  AGENT 1                    │    │
    │  │  Scrapes website            │    │
    │  │  Updates database           │    │
    │  └──────────┬──────────────────┘    │
    │             │                        │
    │             ▼                        │
    │  ┌─────────────────────────────┐    │
    │  │  DATABASE                   │    │
    │  │  - New records added        │    │
    │  │  - Existing updated         │    │
    │  └──────────┬──────────────────┘    │
    │             │                        │
    │      ┌──────┴────────┐              │
    │      │               │              │
    │      ▼               ▼              │
    │  ┌────────┐    ┌──────────┐        │
    │  │ AGENT 2│    │ AGENT 3  │        │
    │  │ Serves │    │ Analyzes │        │
    │  │ Charts │    │ & Alerts │        │
    │  └────┬───┘    └─────┬────┘        │
    │       │              │              │
    │       ▼              ▼              │
    │  ┌────────────────────────────┐    │
    │  │  FRONTEND DISPLAY          │    │
    │  │  - Charts                  │    │
    │  │  - Alerts                  │    │
    │  │  - Statistics              │    │
    │  └────────────────────────────┘    │
    └──────────────────────────────────────┘
```

## 🎨 Frontend Component Tree

```
GoldPriceAnalyzer (Main Component)
│
├── Header
│   ├── Title & Icon
│   └── Sync Button
│       └── onClick → syncData()
│
├── Current Price Display
│   ├── Card: 22 Carat 1 Gram
│   ├── Card: 22 Carat 8 Grams
│   ├── Card: 24 Carat 1 Gram
│   └── Card: Gold Ounce
│
├── Alerts Section
│   └── Alert Cards (map)
│       ├── Icon (based on severity)
│       ├── Message
│       └── Details
│
├── Chart Section
│   ├── Controls
│   │   ├── Period Buttons
│   │   │   └── onClick → setSelectedPeriod()
│   │   └── View Mode Buttons
│   │       └── onClick → setViewMode()
│   │
│   └── Chart (Recharts)
│       ├── AreaChart
│       ├── XAxis
│       ├── YAxis
│       ├── Tooltip
│       └── Legend
│
└── Statistics Section
    ├── Current Price
    ├── Average Price
    ├── Min Price
    ├── Max Price
    └── Change (with %)
```

## 📊 Database Schema Visual

```
┌─────────────────────────────────────┐
│         gold_prices                 │
├─────────────┬───────────────────────┤
│ id          │ 1, 2, 3, ...          │
│ date        │ 2025-10-15            │
│ gold_ounce  │ 1,265,227.00          │
│ carat_24_1g │ 44,630.00             │
│ carat_22_1g │ 40,920.00 ← Main!     │
│ carat_22_8g │ 327,300.00            │
│ carat_21_1g │ 39,060.00             │
│ created_at  │ 2025-10-15 06:00:00   │
│ updated_at  │ 2025-10-15 06:00:00   │
└─────────────┴───────────────────────┘

┌─────────────────────────────────────┐
│       price_alerts                  │
├──────────────┬──────────────────────┤
│ id           │ 1, 2, 3, ...         │
│ alert_type   │ 'increase'/'decrease'│
│ percentage   │ 13.67                │
│ base_price   │ 36,000.00            │
│ current_price│ 40,920.00            │
│ date_triggered│ 2025-10-15          │
│ period_type  │ 'week'/'month'       │
│ is_critical  │ 1 (true) or 0 (false)│
│ created_at   │ 2025-10-15 06:00:00  │
└──────────────┴──────────────────────┘
```

## 🎯 User Interaction Flow

```
1. USER OPENS APP
   ↓
   Frontend loads
   ↓
   Fetches /api/dashboard
   ↓
   Displays current state

2. USER CLICKS "SYNC DATA"
   ↓
   POST /api/scrape/sync
   ↓
   Agent 1 scrapes website
   ↓
   Database updated
   ↓
   Success message shown

3. USER SELECTS "6 MONTHS"
   ↓
   GET /api/prices/period/6months
   ↓
   Agent 2 queries database
   ↓
   Chart re-renders with new data

4. USER SWITCHES TO "MONTHLY VIEW"
   ↓
   GET /api/prices/monthly-aggregate
   ↓
   Agent 2 calculates monthly averages
   ↓
   Chart shows aggregated data

5. USER HOVERS OVER CHART
   ↓
   Tooltip shows exact values
   ↓
   Date, price, and details displayed

6. ALERT IS TRIGGERED
   ↓
   Agent 3 detects threshold breach
   ↓
   Alert saved to database
   ↓
   Alert displayed with color coding
```

## 🚀 Performance Optimization Flow

```
Request → FastAPI
            │
            ├→ Check if data in DB?
            │  │
            │  ├→ Yes → Agent 2 serves from DB (FAST)
            │  │
            │  └→ No → Agent 1 scrapes website (SLOW)
            │           ↓
            │           Stores in DB
            │           ↓
            │           Future requests are FAST
            │
            └→ Return response (JSON)
                ↓
              Frontend renders
```

## 🎨 Color Coding System

```
ALERTS:
┌────────────────────────────────────┐
│ 🔴 CRITICAL DECREASE (≥ 5%)       │
│    Red background, red icon        │
│    "⚠️ Price dropped X%"           │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│ 🟢 CRITICAL INCREASE (≥ 10%)      │
│    Green background, green icon    │
│    "🚀 Price increased X%"         │
└────────────────────────────────────┘

┌────────────────────────────────────┐
│ 🔵 INFO (Normal changes)          │
│    Blue background, blue icon      │
│    "ℹ️ Weekly/Monthly: +/-X%"     │
└────────────────────────────────────┘

PRICE CARDS:
┌────────────────────────────────────┐
│ 🟡 Amber gradient (22 Carat 1g)   │
│ 🟠 Orange gradient (22 Carat 8g)  │
│ 🟡 Yellow gradient (24 Carat 1g)  │
│ 🔴 Red gradient (Gold Ounce)      │
└────────────────────────────────────┘
```

---

This visual guide helps understand how all components work together in the Gold Price Analyzer system!
